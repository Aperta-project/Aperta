<!DOCTYPE html>
<html>
  <head>
    <style>
     .divider:nth-child(odd) {
       border-top: thick double;
       border-bottom: thick double;
       background-color: #e3e3e3;
     }
    </style>
    <title><%= "test" %></title>
  </head>
  <body>
    <%= render partial: "export/content", object: @content, locals: {owner: @owner} %>
    <% if @owner.try(:comments).present? && @owner.comments.size > 0 %>
      <h2>Discussions</h2>
      <div>
        <%= render partial: "export/comment", collection: @owner.comments %>
      </div>
    <% end %>
  </body>
</html>
