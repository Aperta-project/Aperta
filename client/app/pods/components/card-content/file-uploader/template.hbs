{{#if (eq content.valueType "attachment")}}
  {{attachment-manager filePath="tasks/attachment"
    attachments=answer.attachments
    buttonText=content.label
    description=content.text
    disabled=disabled
    preview=preview
    multiple=content.allowMultipleUploads
    hasCaption=content.allowFileCaptions
    errorMessage=content.errorMessage
    accept=acceptedFileTypes
    uploadFinished=(action "updateAttachment")
    updateAttachment=(action "updateAttachment")
    answer=answer
    content=content}}
{{else}}
  {{paper-attachment-manager
    task=owner
    attachmentType=content.valueType
    disabled=disabled
    preview=preview
    answer=answer
    content=content
    showDescription=true
    buttonText=content.label
    description=content.text
    errorMessage=content.errorMessage
    accept=acceptedFileTypes}}
{{/if}}
