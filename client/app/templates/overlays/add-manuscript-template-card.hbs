<div class="overlay-container full">

  <div class="overlay-title-text">
    <h1>Pick the type of card to add</h1>
  </div>

  <div class="inner-content">
    {{#if formattedTaskTypesReady}}
      {{select-2-single placeholder="Select a task type"
                        selectedData=selectedData
                        selectionSelected="taskTypeSelected"
                        source=formattedTaskTypes
                        class="task-type-select"
                        value=""
                        width="200"}}
    {{/if}}

    <div class="overlay-action-buttons">
      <button class="button-primary button--green" {{action "addTaskType" phaseTemplate taskType}}>Add</button>
      <button class="button-link button--green cancel" {{action "closeAction"}}>cancel</button>
    </div>
  </div>
</div>
