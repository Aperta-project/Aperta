<div class="rescind-decision-container">

A decision of <span class="rescind-decision-verdict">{{humanize decision.verdict}}</span> has been made.

{{#if decision.rescindable}}
<button disabled={{not isEditable}}
                    {{action "confirmRescind"}}
                    class="rescind-decision-button">
  <i class="fa fa-undo"></i>
  Rescind
</button>
{{/if}}

{{#if confirmingRescind}}
  {{#full-overlay-verification confirmText="Rescind decision"
                               confirm=(action "rescind")
                               cancel=(action "cancel")}}
    "This will rescind the decision of
    <span class="rescind-decision-verdict">{{humanize decision.verdict}}</span>.
    Are you sure?"
  {{/full-overlay-verification}}
{{/if}}
</div>
