#= require test_helper

moduleFor 'route:profile', 'Unit: route/Profile',
  needs: ['model:currentUser', 'route:profile'],
  # setup: ->
    # @subject().store = find: sinon.stub()
describe 'blah', ->
  it 'blahs', ->
    debugger
# test 'the model should be paper', ->
#   debugger
#   @subject().model paper_id: 123
#   ok @subject().store.find.calledWith 'paper', 123
