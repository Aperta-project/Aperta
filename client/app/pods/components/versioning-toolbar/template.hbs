<div id="versioning-toolbar" {{bind-attr class="versioningMode:versioning-mode-open:versioning-mode-closed"}}>
  <div {{bind-attr class=":versioning-toolbar-limit versioningModeTransition:versioning-mode-open:versioning-mode-closed"}}>
    <div class="versioning-toolbar-background">
      {{#if versioningModeTransition}}
        {{#if togglable}}
          <div class="versioning-mode-button toolbar-item" {{action "closeVersioningMode"}}>
            Exit Versions
            <div class="fa fa-large fa-chevron-circle-right"></div>
          </div>
        {{/if}}
        <div class="toolbar-item">
          Now viewing:
          {{view "select"
            content=paper.versions
            optionValuePath="content.id"
            optionLabelPath="content.name"
            selection=selectedVersion}}
        </div>
      {{else}}
        <div class="versioning-mode-button" {{action "openVersioningMode"}}>
          <div class="fa fa-large fa-chevron-circle-left"></div>
          View Versions
        </div>
      {{/if}}
    </div>
  </div>
</div>
