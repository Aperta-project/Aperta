<div class="overlay-container">
  <header class="overlay-header">
    {{#if title}}
      <h1 class="overlay-header-title">{{title}}</h1>
    {{/if}}

    <span class="overlay-close" {{action "close"}}>&times;</span>
  </header>

  <div class="overlay-body">
    {{yield}}
  </div>
</div>
