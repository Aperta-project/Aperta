<div class="back-link">
  {{#link-to "admin.journal.index" journal class="button-link button--blue"}}&lt; Back{{/link-to}}
  Flow Manager for {{model.name}} Role
</div>
<div class="flow-admin-content">
  <ul class="columns flow-admin">
    {{#each sortedFlows as |flow|}}
      {{flow-column flow=flow
                    paper=paper
                    commentLooks=commentLooks
                    journalTaskTypes=journalTaskTypes
                    editable=true
                    removeFlow="removeFlow"
                    saveFlow="saveFlow"
                    viewCard="viewCard"
                    journalName=journal.name
                    journalLogo=journal.logoUrl}}
    {{/each}}
  </ul>
</div>
