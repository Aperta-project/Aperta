## Customize machine
machine:
  timezone:
    America/Los_Angeles

  ruby:
    version:
      2.2.0

  environment:
    RAILS_ENV: test
    DATABASE_URL: postgres://ubuntu:@127.0.0.1:5432/circle_test


## Customize dependencies
dependencies:
  cache_directories:
    - "~/.rvm/gems/ruby-2.2.0@tahi"
    - "client/node_modules"
    - "client/bower_components"

  pre:
    - bundle config --global jobs 7
    - bundle config --global without staging:production:performance

  override:
    - bundle install: # note ':' here
        timeout: 600


## Customize database setup
database:
  pre:
    # install custom postgres extensions
    - psql -c "CREATE EXTENSION IF NOT EXISTS plpgsql;" -d circle_test
    - psql -c "CREATE EXTENSION IF NOT EXISTS pg_trgm;" -d circle_test
    - psql -c "CREATE EXTENSION IF NOT EXISTS unaccent;" -d circle_test
  override:
    - bundle exec rake db:create db:schema:load
  post:
    # npm install here so that it is cached by circle for parallel tests
    - npm install && bundle exec rake ember:install


## Customize test commands
test:
  override:
    # run only our tests, not all tahi test
    - "bundle exec rspec --format documentation -P \"`bundle show tahi-assess`/spec/**/*_spec.rb,spec/**/*_spec.rb\" --exclude-pattern \"spec/features/**/*.rb\""

## Customize heroku deployment
deployment:
  staging:
    branch: assess
    commands:
      - git push git@heroku.com:tahi-assess.git -f $CIRCLE_SHA1:refs/heads/master
      - heroku run rake db:migrate --app tahi-assess
