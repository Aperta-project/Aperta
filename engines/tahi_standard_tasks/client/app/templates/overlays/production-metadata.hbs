<h1>
  Production Metadata
</h1>

<div class='production-metadata'>
  <div class="form-group">
    <div class="inset-form-control required publication-date {{if validationErrors.publicationDate "error"}}">
      {{error-message message=validationErrors.publicationDate class="inset-form-control-text"}}
      <label class="inset-form-control-text">Publication Date</label>
      {{question-date-picker ident="production_metadata.publicationDate"
                             task=model
                             group=group
                             role="startPicker"
                             placeholder="Select Date..."}}
    </div>
  </div>
  <div class="form-group {{if validationErrors.volumeNumber "error"}}">
    <div class="inset-form-control required volume-number">
      {{error-message message=validationErrors.volumeNumber class="inset-form-control-text"}}
      <label class="inset-form-control-text">Volume Number</label>
      {{question-input  ident="production_metadata.volumeNumber"
                        task=model
                        type="number"
                        placeholderText="e.g. 3"}}
    </div>

    <div class="inset-form-control required issue-number {{if validationErrors.issueNumber "error"}}">
      <label class="inset-form-control-text">Issue Number</label>
      {{error-message message=validationErrors.issueNumber class="inset-form-control-text"}}
      {{question-input  ident="production_metadata.issueNumber"
                        task=model
                        type="number"
                        placeholderText="e.g. 33"}}
    </div>
  </div>

  <div class="production-notes">
    <label>Production Notes</label>
    {{question-textarea ident="production_metadata.productionNotes"
                        task=model
                        label="Production Notes"
                        placeholder="Add production notes here..."}}
  </div>
</div>
