import OverlayView from 'tahi/views/overlay';

var SendToApexOverlayView = OverlayView.extend({
  templateName: 'overlays/send_to_apex',
  layoutName: 'layouts/overlay'
});

export default SendToApexOverlayView;
