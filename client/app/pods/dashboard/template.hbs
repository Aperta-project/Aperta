{{view "control-bar"}}
{{flash-messages classNames="flash-messages--below-control-bar"}}

<div id="tahi-container">
  <section id="dashboard-my-submissions">

    {{#if pendingInvitations.length}}
      <h2 class="welcome-message">
        You have {{model.invitations.length}} {{make-plural pendingInvitations.length "invitation" "invitations"}}.
      </h2>
      <button class="button-primary button--green" {{action "viewInvitations" pendingInvitations}}>View invitations</button>
    {{/if}}

    <h2 class="welcome-message">
      {{#if papers.length}}
        Hi, {{currentUser.firstName}}.  You have {{totalPaperCount}} {{make-plural totalPaperCount "manuscript" "manuscripts"}}.
      {{else}}
        You have no manuscripts.
      {{/if}}
    </h2>

    <button class="button-primary button--green" {{action "showNewPaperOverlay"}}>Create New Submission</button>
    {{#if hasPapers}}
      <ul class="dashboard-submitted-papers">
        {{#each paper in sortedPapers}}
          {{dashboard-link model=paper unreadComments=unreadComments}}
        {{/each}}
      </ul>
      {{#if paginate}}
        <a {{action "loadMorePapers"}} href="#" class="button-secondary button--green load-more-papers">
          Load More
        </a>
      {{/if }}
    {{else}}
      <p class="dashboard-info-text">Your scientific paper submissions will<br> appear here.</p>
    {{/if}}
  </section>
</div>
