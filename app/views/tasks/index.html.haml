:coffeescript
  window.onresize = ->
    Tahi.utils.resizeContainer('.phase-container','h2')

  $ ->
    Tahi.utils.resizeContainer('.phase-container', 'h2')
    # fix for safari.
    setTimeout( ->
      Tahi.utils.resizeContainer('.phase-container', 'h2')
    ,200)

#manuscript-manager

- content_for :tahi_container_class do
  full-width
