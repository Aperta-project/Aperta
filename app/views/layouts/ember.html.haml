!!!
%html
  %head
    %title Tahi
    = stylesheet_link_tag    "application", media: "all"
    = stylesheet_link_tag "http://fonts.googleapis.com/css?family=Cabin:400,700,400italic"
    = javascript_include_tag "application"
    = csrf_meta_tags

  %body.full-height
    #ember-app

- if current_user
  :javascript
    window.Tahi.currentUser = {id: #{current_user.id}, admin: #{current_user.admin}, fullName: "#{current_user.full_name}", imageUrl: "#{current_user.image_url}"};
