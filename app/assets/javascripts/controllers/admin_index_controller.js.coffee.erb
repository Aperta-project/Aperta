ETahi.AdminIndexController = Ember.ArrayController.extend
  actions:
    addNewJournal: ->
      @store.createRecord 'adminJournal', logoUrl: "<%= asset_path('no-journal-image.gif') %>"

    searchUsers: ->
      @set 'adminJournalUsers', @store.find('AdminJournalUser', {query: @get('searchQuery')})
