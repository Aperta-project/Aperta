<div id="versioning-bar">
  <div class="versioning-bar-container">
    <div class="bar-item">
      Now viewing:
      {{select-native name="view_version"
                      content=paper.versions
                      optionValuePath="id"
                      optionLabelPath="name"
                      selection=viewingVersion
                      action=(action (mut viewingVersion))}}
    </div>
    <div class="bar-item">
      Compare With:
      {{select-native name="compare_version"
                      prompt="-"
                      content=paper.versions
                      optionValuePath="id"
                      optionLabelPath="name"
                      selection=comparisonVersion
                      action=(action (mut comparisonVersion))}}
    </div>
  </div>
</div>
