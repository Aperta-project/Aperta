<%= render partial: 'email/journal_logo_or_name', locals: { journal: @paper.journal } %>
<br><br>

<div>
  <%= @invitation.body.html_safe %>
</div>

<%= render partial: 'email/button', locals: {
  href: invitation_accept_url({ token: @invitation.token }),
  text: 'Accept Invitation'
} %>

<%= render partial: 'email/button', locals: {
  href: voucher_invitation_url({ id: @invitation.token }).gsub('_', '-').gsub('api/', ''),
  text: 'Decline'
} %>
