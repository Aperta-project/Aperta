{{#if paperSubmitted}}

  {{#if model.gradualEngagement}}

    {{#if model.isInitiallySubmitted}}
      {{partial 'overlays/paper-submit/initial-submit'}}
    {{else if isFirstFullSubmission}}
      {{partial 'overlays/paper-submit/full-submit'}}
    {{else if model.isSubmitted}}
      {{partial 'overlays/paper-submit/standard-submit'}}
    {{/if}}

  {{else}}
    {{partial 'overlays/paper-submit/standard-submit'}}
  {{/if}}

{{else}}
  {{partial 'overlays/paper-submit/about-to-submit'}}
{{/if}}
