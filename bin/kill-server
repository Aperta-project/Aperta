#!/bin/bash

if [ -n "$(lsof -ti :8080)" ]
  then
    kill -9 `lsof -ti :8080`
fi

if [ -n "$(lsof -ti :5000)" ]
  then
    kill -9 `lsof -ti :5000`
fi

if [ -n "$(lsof -ti :6379)" ]
  then
    kill -9 `lsof -ti :6379`
fi

if [ -n "$(lsof -ti :8982)" ]
  then
    kill -9 `lsof -ti :8982`
fi
