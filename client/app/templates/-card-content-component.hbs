{{#if hasBlock}}
  {{yield (hash
  content=content
  disabled=disabled
  owner=owner
  answer=answer
  preview=preview
  position=position
  answerChanged=answerChanged
  valueChanged=(action 'updateAnswer' answer))}}
{{else}}
  {{component
  templateName
  content=content
  disabled=disabled
  owner=owner
  answer=answer
  preview=preview
  position=position
  answerChanged=answerChanged
  valueChanged=(action 'updateAnswer' answer)}}
{{/if}}
