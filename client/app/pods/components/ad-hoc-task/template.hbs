{{partial "task-completed-section"}}

{{ad-hoc-body
    task=task
    blocks=task.body
    canEdit=(can 'view' task)
    canManage=(can 'edit' task)
    isEditingTemplate=false
    save=(action 'save')
    sendEmail=(action 'sendEmail')}}
