{{#if hasPrev}}
  <button class='btn btn-link prev' {{action 'prev'}}> <i class='fa fa-chevron-circle-left'></i> Prev </button>
{{/if}}

Page {{page}} of {{pagesUI}}

{{#if hasNext}}
  <button class='btn btn-link next' {{action 'next'}}>Next <i class='fa fa-chevron-circle-right'></i></button>
{{/if}}
