<h1>{{model.title}}</h1>
<ul class="participants">
  {{#each p in participants}}
    <li class="participant">{{userThumbnail p}}</li>
  {{/each}}
  <li class="chosen-style-override participant">
    {{chosen content=availableParticipants
             name="participants"
             width="150px"
             prompt="Add People"
             class="participant-select"
             changeAction="saveNewParticipant"
             optionValuePath="content"
             optionLabelPath="content.fullName"}}
  </li>
</ul>

{{#unless showAllComments}}
  <div class="comment-actions" {{action "showAllComments"}}>
    <a class="load-all-comments" href="#">
      <span class="glyphicon glyphicon-comment"></span>
      View previous {{omittedCommentsCount}} {{pluralize omittedCommentsCount "comment" "comments"}}
    </a>
  </div>
{{/unless}}

<ul class="message-comments">
  {{#each comment in sortedComments}}
    {{ render 'comment' comment }}
  {{/each}}
</ul>

<div class="form-group">
  {{textarea classNames="new-comment" id="comment-body" value=newCommentBody placeholder="Type your message here"}}
</div>

<div class="message-comment-buttons">
  <a href="#" {{action "clearMessageContent"}}>Cancel</a>
  <button type="submit" class="secondary-button" {{action "postComment"}}>Post Message</button>
</div>
