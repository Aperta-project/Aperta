{{#unless hideCompletedSection}}
  {{partial "task-completed-section"}}
{{/unless}}
<div class="custom-card-task {{if renderAsDualColumn 'dual-column' 'single-column'}}">
  {{card-content
    content=contentRoot
    owner=task
    disabled=isNotEditable
    scenario=this
  }}
</div>
