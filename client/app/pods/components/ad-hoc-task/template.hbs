{{partial "task-completed-section"}}

{{ad-hoc-body
    task=task
    blocks=task.body
    canEdit=(can 'edit' task)
    canManage=(can 'manage' task)
    isEditingTemplate=false
    save=(action 'save')
    sendEmail=(action 'sendEmail')}}
