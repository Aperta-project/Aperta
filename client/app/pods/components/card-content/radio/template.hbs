<p class="content-text">{{{content.text}}}
  {{#if content.isRequired}}
    {{#unless answer.wasAnswered}}<span class="required-field">*</span>{{/unless}}
  {{/if}}
</p>
<div>
  {{#if isText}}
    {{#each content.possibleValues as |opt index|}}
      <label for="{{input-id this index}}" class="option">
        {{radio-button
          selection=answer.value
          value=opt.value
          disabled=disabled
          id=(input-id this index)
          content=content
          action=(action "valueChanged")}}
        <span class="card-content-radio-label">{{{opt.label}}}</span>
      </label>
    {{/each}}
  {{else }}
    <label for="{{input-id this '11'}}" class="option">
      {{radio-button
        selection=answer.value
        value=true
        id=(input-id this '11')
        disabled=disabled
        content=content
        action=(action "valueChanged")}}
      <span class="card-content-radio-label">Yes</span>
    </label>
    <label  for="{{input-id this '22'}}" class="option">
      {{radio-button
        selection=answer.value
        value=false
        id=(input-id this '22')
        disabled=disabled
        content=content
        action=(action "valueChanged")}}
      <span class="card-content-radio-label">No</span>
    </label>
  {{/if}}
</div>

{{card-content/display-children
  content=content
  owner=owner
  disabled=disabled
  preview=preview}}
