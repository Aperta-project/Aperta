import Ember from 'ember';
import { test, moduleForComponent } from 'ember-qunit';

moduleForComponent('nested-question', 'Unit | Component | nested question', {
  unit: true,
});

test('redo all these tests for the new behavior', function(assert) { 
  assert.ok(false);
});
