## Customize machine
machine:
  timezone:
    America/Los_Angeles

  ruby:
    version:
      2.2.8 # when changing ruby versions, also update the cache_directory path below
  node:
    version: 6.11.1

  environment:
    RAILS_ENV: test
    DATABASE_URL: postgres://ubuntu:@127.0.0.1:5432/circle_test
    FIREFOX_URL: https://ftp.mozilla.org/pub/firefox/releases/54.0.1/linux-x86_64/en-US/firefox-54.0.1.tar.bz2
    FF_FILE: ff/firefox-54.0.1.tar.bz2

## Customize dependencies
dependencies:
  cache_directories:
    - "/opt/circleci/.rvm/gems/ruby-2.2.8@tahi"
    - "client/node_modules"
    - "client/bower_components"
    - ff

  pre:
    - gem update --system
    - gem install bundler -v '1.15.4'
    - bundle config --global jobs 7
    - bundle config --global without staging:production
    - "[ -f $FF_FILE ] || wget $FIREFOX_URL -qP ff"
    - tar xvf $FF_FILE

  override:
    - bundle install: # note ':' here
        timeout: 600


## Customize database setup
database:
  pre:
    # install custom postgres extensions
    - psql -c "CREATE EXTENSION IF NOT EXISTS plpgsql;" -d circle_test
    - psql -c "CREATE EXTENSION IF NOT EXISTS pg_trgm;" -d circle_test
    - psql -c "CREATE EXTENSION IF NOT EXISTS unaccent;" -d circle_test
  override:
    - bundle exec rake db:create db:schema:load
  post:
    - bundle exec rake ember:install:
        timeout: 900 # 15 minutes


## Customize test commands
test:
  override:
    - bundle exec rake circleci:qunit:
       parallel: true
    - bundle exec rspec -p 30 -t ~js --format RspecJunitFormatter --out $CIRCLE_TEST_REPORTS/rspec-unit.xml:
        timeout: 1800
        parallel: true
        files:
          - "**/spec/**/*_spec.rb"
    - bundle exec rspec -p 30 -t js --format RspecJunitFormatter --out $CIRCLE_TEST_REPORTS/rspec-features.xml:
        environment:
          SELENIUM_FIREFOX_PATH: $PWD/firefox/firefox
        timeout: 1800
        parallel: true
        files:
          - "**/spec/**/*_spec.rb"
