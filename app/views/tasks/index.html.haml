#manuscript-manager{data: {url: manage_paper_path(@paper)}}

:javascript
  window.Tahi.currentUser = {id: #{current_user.id}, fullName: "#{current_user.full_name}", imageUrl: "#{current_user.image_url}"}

- content_for :tahi_body_class do
  full-height

- content_for :tahi_container_class do
  full-width
