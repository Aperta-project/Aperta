{{#if disabled}}
  <div class="read-only">
    {{{value}}}
  </div>
{{else}}
  {{tinymce-editor
    class=class
    name=name
    onValueChanged=(action onContentsChanged)
    options=editorOptions
    placeholder=placeholder
    value=value
  }}
{{/if}}
