{{#if shouldDisplayQuestionText}}
  <span class="{{textClassNames}}">
    {{{questionText}}}
  </span>
{{/if}}

<div class="additional-data">
  <span class="button-primary button--green fileinput-button {{if disabled 'button--disabled'}}">
    {{if model.answer.value "Replace File" "Select & upload"}}

    {{file-uploader accept=accept
                    start="uploadStarted"
                    progress="uploadProgress"
                    done="uploadFinished"
                    error="uploadError"
                    filePrefix="question_attachment"
                    disabled=disabled}}
  </span>
  {{#if fileUpload}}
    <div id="paper-file-uploads">
      {{upload-preview upload=fileUpload
                       disabled=disabled}}
    </div>
  {{/if}}
  {{#if model.answer.value}}
    {{attachment-thumbnail attachment=model.answer.value
                           destroyAttachment="destroyAttachment"}}
  {{/if}}
</div>
