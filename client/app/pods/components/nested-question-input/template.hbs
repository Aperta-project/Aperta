{{#if shouldDisplayQuestionText}}
  <span class="{{textClassNames}}">
    {{{questionText}}}
  </span>
{{/if}}

{{#if hasBlock}}
  {{yield textYieldValue}}
{{/if}}

<div>
  {{#if label}}<span class="text-field-title">{{label}}</span>{{/if}}
  {{input name=ident
          value=model.answer.value
          class=inputClassNames
          placeholder=placeholderText
          disabled=disabled}}
</div>
