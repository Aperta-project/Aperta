Feedback from:
<%= @resource.user.username %>

On the page:
<%= @resource.referrer %>

Current Rails environment:
<%= @resource.env %>

Body:
<%= @resource.feedback %>

Screenshots:
<% @resource.screenshots.each do |screenshot| %>
  <%= image_tag screenshot["url"] %>
<% end %>
