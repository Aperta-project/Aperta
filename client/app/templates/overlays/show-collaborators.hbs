<div id="show-collaborators">
  <header class="overlay-title-text">
    <h1 style="padding-bottom: 25px;">
      Who can collaborate on this manuscript?
    </h1>
  </header>

  <p class="overlay-supporting-text">
    Select people to collaborate with on this paper.
    Collaborators can edit the paper,
    will be notified about edits on the paper,
    and can participate in the discussion about this paper.
  </p>

  <div class="call-to-action">
    {{select-2-single class="collaborator-select"
                      name="collaborators"
                      placeholder="Add collaborator"
                      selectionSelected="addNewCollaborator"
                      source=formattedCollaborators
                      value=""}}

    <div class="overlay-action-buttons">
      <a class="button-link button--green" {{action "cancel"}}>cancel</a>
      <button class="button-primary button--green" {{action "save" phase}}>Save</button>
    </div>
  </div>

  <ul class="collaborators-list">
    {{#each collaborators as |collaborator|}}
      <li class="collaborator">
        <span class="avatar">{{user-thumbnail user=collaborator}}</span>
        <span class="name">{{collaborator.fullName}}</span>
        <span class="email">{{collaborator.email}}
          <span class="delete-button fa fa-trash" {{action "removeCollaborator" collaborator}}>
          </span>
        </span>
      </li>
    {{/each}}
  </ul>
</div>
